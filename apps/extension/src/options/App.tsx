import { AppShell } from '../components/AppShell';

export function App() {
  return (
    <AppShell title="Career Finder — Options">
      <p>
        Options page scaffold — the Support/donate section (Stage 3) and any settings land here. See{' '}
        <code>docs/DEVELOPMENT_PLAN.md</code>.
      </p>
    </AppShell>
  );
}
